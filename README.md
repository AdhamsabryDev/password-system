# password-system
A lightweight Python authentication script that validates passwords, tracks remaining attempts, provides dynamic feedback, and terminates execution upon reaching the attempt limit using standard control flow constructs.
